export type RegisterModalinputs = {
    name: string;
    email: string;
    password: string;
    avatar: string;
};

export type inputsBskDevice = {
    size: string;
    color: string;
    count: number;
}

export  type LoginModalFormInputs = {
    email: string;
    password: string;
}